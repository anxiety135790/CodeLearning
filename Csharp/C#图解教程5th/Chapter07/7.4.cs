class D
{
    int Mem1;   // 1. Instance field
    static int Mem2; // 2. Static field

    static void Main()
    {
        D d1 = new();   // 3. Instance object
        D d2 = new(); // 4. Instance object

    }
}